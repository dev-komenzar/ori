{
  "name": "@ori-ori/arch-adapter-eslint-bundled",
  "version": "0.0.0",
  "private": true,
  "description": "Source for the eslint arch adapter — built to .apm/contexts/adapters/eslint/ via scripts/build-adapters.mjs",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ori-ori/parser": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
