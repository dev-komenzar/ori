{
  "name": "@ori-ori/arch-adapter-rust-bundled",
  "version": "0.0.0",
  "private": true,
  "description": "Source for the rust arch adapter — built to .apm/contexts/adapters/rust/ via scripts/build-adapters.mjs",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ori-ori/parser": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
