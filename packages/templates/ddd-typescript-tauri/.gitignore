node_modules/
dist/
coverage/

# Auto-generated by `pnpm arch:export:ts` / `pnpm arch:export:rs`.
# Architecture.md is the SSoT — these files can be regenerated any time.
eslint.config.ori.js
src-tauri/tests/arch.rs

# Tauri build artifacts
src-tauri/target/
src-tauri/gen/

# tauri-specta regenerates this on each `pnpm tauri dev|build`.
# Commit it or not — your call (committed by default so the project
# type-checks before the first Rust build).
# src/lib/shared/ipc/bindings.ts
