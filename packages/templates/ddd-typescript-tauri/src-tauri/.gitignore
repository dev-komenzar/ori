# Cargo build artifacts
target/

# tauri-build cache
gen/

# Generated arch test (regenerate with `ori arch export --adapter=rust --root=rs`)
tests/arch.rs

# Local IDE / OS noise
*.bk
*.swp
.DS_Store
